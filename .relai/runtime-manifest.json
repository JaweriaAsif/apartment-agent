{
  "schema_version": "relai.runtime_manifest.v1",
  "env": [
    {
      "name": "OPENAI_API_KEY",
      "required": true,
      "sensitive": true,
      "reason": "Required by the OpenAI Agents SDK client before the simulator can run the apartment search agent."
    },
    {
      "name": "OPENAI_MODEL",
      "required": false,
      "sensitive": false,
      "reason": "Optional override for the model passed into apartment_agent.agent.build_agent(); defaults to gpt-5.4 when unset."
    }
  ],
  "files": []
}
